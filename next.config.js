// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Only add domains if you use external images
    // domains: ["your-cdn.example.com"],
  },
};

module.exports = nextConfig;
